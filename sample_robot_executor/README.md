# sample_robot_executor

An executor of the sample robot in Gazebo simulator

## Usage

```sh
$ export TURTLEBOT3_MODEL=waffle
$ roslaunch sample_robot_executor wg_world.launch
$ roslaunch sample_robot_executor bringup.launch
```
